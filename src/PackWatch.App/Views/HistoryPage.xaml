<UserControl x:Class="PackWatch.App.Views.HistoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="History"
                   Style="{StaticResource PageTitleStyle}" />

        <Border Grid.Row="1"
                Background="{StaticResource PackWatchSurfaceBrush}"
                BorderBrush="{StaticResource PackWatchBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Date Filter" Foreground="{StaticResource PackWatchMutedTextBrush}" />
                    <ComboBox SelectedValue="{Binding SelectedDateFilter}" SelectedValuePath="Content" Margin="0,6,0,0">
                        <ComboBoxItem Content="Today" />
                        <ComboBoxItem Content="Yesterday" />
                        <ComboBoxItem Content="This Week" />
                        <ComboBoxItem Content="Custom Date" />
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Search Order" Foreground="{StaticResource PackWatchMutedTextBrush}" />
                    <TextBox Text="{Binding SearchOrderCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource PackWatchSurfaceBrush}"
                BorderBrush="{StaticResource PackWatchBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <TextBlock Text="{Binding EmptyStateMessage}"
                       Foreground="{StaticResource PackWatchMutedTextBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
        </Border>
    </Grid>
</UserControl>
